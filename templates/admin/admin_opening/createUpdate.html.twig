{% extends 'base.html.twig' %}

{% block css %} <link rel="stylesheet" href="{{ asset('assets/css/createUpdateOpening.css') }}"> {% endblock css %}


{% block title %} Modification/création d'un horaire {% endblock %}


{% block main %}

    {# Create or update opening hours section #}

    <section id="update-opening">
        <div class="row update-opening">

            <article class="container my-4">
                <div class="row text-white d-flex justify-content-center mx-1">

                    {# Message flash #}

{#                    <div class="col-md-8 confirm-message text-center">#}
{#                        {% for label, messages in app.flashes %}#}
{#                            {% for message in messages %}#}
{#                                <div class="alert alert-{{ label }} alert-dismissible fade show font-weight-bold" role="alert">#}
{#                                    {{ message }}#}
{#                                    <button type="button" class="close" data-dismiss="alert" aria-label="Close"></button>#}
{#                                </div>#}
{#                            {% endfor %}#}
{#                        {% endfor %}#}
{#                    </div>#}

                    <div class="col-md-10 form-bg rounded p-2 p-md-4 my-5">

                        {# Page title #}

                        <div class="col-12 py-3">
                            {% if isUpdated %}
                                <h1 class="page-title">Modification d'un horaire</h1>
                                <h2 class="text-center">Modification d'un horaire</h2>
                            {% else %}
                                <h1 class="page-title">Ajout d'un horaire</h1>
                                <h2 class="text-center">Ajout d'un horaire</h2>
                            {% endif %}
                        </div>

                        {# Form #}

                        <div class="col-12">
                            {{ form_start(form) }}

{#                            {{ form_widget(form) }}#}

                            <div class="row d-flex align-items-center">

                                <div class="col-md-5">
                                    {{ form_row(form.day) }}
                                </div>

                                <div class="col-md-2">
                                    {{ form_row(form.open) }}
                                </div>

                                <div class="col-md-5">
                                    {{ form_row(form.restaurant) }}
                                </div>
                            </div>

                            <div class="row">

                                <div class="col-md-12">

                                    <div class="row ">
                                        {% for openinghour in form.openinghours %}
                                        <div class="col-md-6">
                                            {{ form_row(openinghour.starthour) }}
                                        </div>
                                        {% endfor %}
                                        {% for openinghour in form.openinghours %}
                                        <div class="col-md-6">
                                            {{ form_row(openinghour.endhour) }}
                                        </div>
                                        {% endfor %}

                                    </div>
                                </div>
                            </div>


                            {# buttons #}
                            <div class="row d-flex">
                                <div class="col-12 d-flex justify-content-center my-4">
                                    {# Come back #}
                                    <a href="{{ path('app_admin_opening') }}" class="btn btn-custom mr-2">Revenir</a>
                                    {% if isUpdated %}
                                        {# Save the updatings #}
                                        <input type="submit" class="btn btn-admin ml-2" value="Modifier">
                                    {% else %}
                                        <input type="submit" class="btn btn-admin ml-2" value="Ajouter">
                                    {% endif  %}
                                </div>
                            </div>

                            {{ form_end(form) }}
                        </div>
                    </div>
                </div>
            </article>
        </div>
    </section>

{% endblock %}
